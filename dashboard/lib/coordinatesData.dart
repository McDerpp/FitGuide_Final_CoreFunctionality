import 'package:flutter/material.dart';

// this will conain data related to coordinates collected
// average luminance level (as requested by sir panelist)
// confidence level
// average sequence length
// min sequence length
// max sequence length
// coordinates ignored
// coordinates variance???? -> if possible
// 


class coordinatesData extends StatefulWidget {
  const coordinatesData({super.key});

  @override
  State<coordinatesData> createState() => _coordinatesDataState();
}

class _coordinatesDataState extends State<coordinatesData> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}